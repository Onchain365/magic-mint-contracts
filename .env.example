# Environment Variables

Copy this file to `.env` and fill in your values. Never commit `.env` to version control.

```
# Your deployer wallet private key (without 0x prefix)
PRIVATE_KEY=your_private_key_here

# BSC RPC URLs (public defaults are already set in hardhat.config.ts)
BSC_RPC_URL=https://bsc-dataseed.binance.org/
BSC_TESTNET_RPC_URL=https://data-seed-prebsc-1-s1.binance.org:8545

# BscScan API key — get one free at https://bscscan.com/apis
ETHERSCAN_API_KEY=your_bscscan_api_key_here
```
